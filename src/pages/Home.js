import React from 'react'
import HomeData from './HomeData'

const Home = () => {
    return (
        HomeData.map((dataObj,index) => {
                return (
                    
                )
        })
    )
}

export default Home